import puppeteer from 'puppeteer'; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:5173', {waitUntil: 'networkidle0'}); await page.evaluate(() => localStorage.setItem('takeover_business_data', 'littEAdUPMyuMEdwgJwV4')); await page.reload({waitUntil: 'networkidle0'}); const html = await page.evaluate(() => document.body.innerHTML); console.log('HTML_LENGTH:', html.length); console.log('HTML:', html.substring(0, 500)); await browser.close(); })();
