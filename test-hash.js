const docs = [{name: 'Sales.csv', month: 'January', year: 2026}, {name: 'Sales.csv', month: 'February', year: 2026}]; docs.forEach(doc => { const seedString = doc.name + (doc.month || '') + (doc.year || ''); let hash = 0; for (let i = 0; i < seedString.length; i++) { hash = ((hash << 5) - hash) + seedString.charCodeAt(i); hash |= 0; } const pr = (Math.abs(hash) % 100) / 100; console.log(doc.month, pr, Math.floor(pr * 200000) + 100000); });
