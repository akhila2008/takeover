import puppeteer from 'puppeteer'; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('pageerror', e => console.log('ERROR:', e.message)); page.on('console', m => m.type() === 'error' && console.log('LOG:', m.text())); await page.goto('http://localhost:4173', {waitUntil: 'networkidle0'}); const html = await page.evaluate(() => document.body.innerHTML); console.log('HTML_LENGTH:', html.length); await browser.close(); })();
