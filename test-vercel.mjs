import puppeteer from 'puppeteer'; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('pageerror', e => console.log('ERROR:', e.message)); page.on('console', m => m.type() === 'error' && console.log('LOG:', m.text())); await page.goto('https://takeover-git-main-akhila2008s-projects.vercel.app', {waitUntil: 'networkidle0'}); await browser.close(); })();
